spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/musicly
    username: postgres
    password: postgres
  devtools:
    add-properties: true

server:
  port: 8080

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: false

management:
  endpoint:
    web:
      exposure:
        include: '*'
